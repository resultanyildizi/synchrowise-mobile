import 'package:flutter/material.dart';

class WaveProgressIndicator extends StatelessWidget {
  const WaveProgressIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
