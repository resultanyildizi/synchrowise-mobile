import 'package:flutter/material.dart';

class SignleTextAreaFieldForm extends StatelessWidget {
  const SignleTextAreaFieldForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
